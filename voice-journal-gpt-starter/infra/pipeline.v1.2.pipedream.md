# Pipeline v1.2 – PipeDream

**Flow:** Custom GPT Action → PipeDream Workflow (transform) → Apps Script Web App → Google Sheet

- PipeDream Endpoint: `<your endpoint>`
- Transform Rules: (list your JSON field mappings)
- Auth: Shared secret from GPT → PipeDream → Apps Script
- Error Handling: retries, logging in PipeDream
- Known Costs: credits may run out

## Tasks
- Document the exact PipeDream steps & node names.
- Paste example payloads for /entry and /status.
